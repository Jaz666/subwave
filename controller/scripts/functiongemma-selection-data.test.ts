import assert from "node:assert/strict";
import test from "node:test";
import { generateSelectionExamples, validateSelectionSets } from "./functiongemma/selection-training-data.js";
test("selector corpus is deterministic, grounded, shuffled and isolated",()=>{const train=generateSelectionExamples("train",320),development=generateSelectionExamples("development",80),checked=validateSelectionSets(train,development);assert.deepEqual(train,generateSelectionExamples("train",320));for(const family of ["exact-id","same-artist","recent-rotation","strict-playlist","strict-show-filter","host-over-guest","quiet-continuity","deliberate-contrast"])assert.ok(checked.families[family]>0);const positions=new Set(train.map(item=>{const id=item.messages[2].tool_calls[0].function.arguments.id;const p=item.messages[1].content;return JSON.parse(p.slice(p.indexOf("{"))).candidates.findIndex((candidate:any)=>candidate.id===id)}));assert.deepEqual([...positions].sort(),[0,1,2]);for(const item of [...train,...development])assert.deepEqual(item.tools.map(tool=>tool.function.name),["done"]);});
